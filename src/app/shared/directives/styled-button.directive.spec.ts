import { StyledButtonDirective } from './styled-button.directive';

describe('StyledButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new StyledButtonDirective();
    expect(directive).toBeTruthy();
  });
});
